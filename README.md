    # n8n PostgreSQL Auto-Installer
    Deploy n8n with PostgreSQL, Nginx, and SSL in one command.
    
    ## Usage
    Run this command on your Ubuntu VPS:
```bash
curl -o install.sh https://raw.githubusercontent.com/im-JvD/n8n-installer/main/install.sh && chmod +x install.sh && sudo ./install.sh

